name: Publish Python Package

on:
  push:
    branches:
      - main
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

jobs:
  publish-testpypi:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    env:
      CMAKE_ARGS: -DSPWRMAP_BUILD_TESTS=OFF
      CIBW_BUILD: "cp39-manylinux_x86_64 cp310-manylinux_x86_64 cp311-manylinux_x86_64 cp312-manylinux_x86_64"
      CIBW_ENVIRONMENT: >
        CMAKE_ARGS="-DSPWRMAP_BUILD_TESTS=OFF"
    steps:
      - uses: actions/checkout@v4
      - id: setup-python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Configure Python for CMake
        run: |
          echo "Python3_ROOT_DIR=${pythonLocation}" >> "$GITHUB_ENV"
          echo "Python_ROOT_DIR=${pythonLocation}" >> "$GITHUB_ENV"
          echo "Python3_EXECUTABLE=${pythonLocation}/bin/python" >> "$GITHUB_ENV"
          echo "Python_EXECUTABLE=${pythonLocation}/bin/python" >> "$GITHUB_ENV"
      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install build cibuildwheel
      - name: Build source distribution
        run: python -m build --sdist
      - name: Build binary wheels
        run: python -m cibuildwheel --output-dir dist
      - name: Publish package to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.TEST_PYPI_API_TOKEN }}
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
          verbose: true

  publish-pypi:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    env:
      CMAKE_ARGS: -DSPWRMAP_BUILD_TESTS=OFF
      CIBW_BUILD: "cp39-manylinux_x86_64 cp310-manylinux_x86_64 cp311-manylinux_x86_64 cp312-manylinux_x86_64"
      CIBW_ENVIRONMENT: >
        CMAKE_ARGS="-DSPWRMAP_BUILD_TESTS=OFF"
    steps:
      - uses: actions/checkout@v4
      - id: setup-python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Configure Python for CMake
        run: |
          echo "Python3_ROOT_DIR=${pythonLocation}" >> "$GITHUB_ENV"
          echo "Python_ROOT_DIR=${pythonLocation}" >> "$GITHUB_ENV"
          echo "Python3_EXECUTABLE=${pythonLocation}/bin/python" >> "$GITHUB_ENV"
          echo "Python_EXECUTABLE=${pythonLocation}/bin/python" >> "$GITHUB_ENV"
      - name: Install build tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install build cibuildwheel
      - name: Build source distribution
        run: python -m build --sdist
      - name: Build binary wheels
        run: python -m cibuildwheel --output-dir dist
      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          verbose: true
